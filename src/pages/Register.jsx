// Register navigates to the same AuthScreen in sign-up mode
import AuthScreen from "../components/auth/AuthScreen";

const Register = () => {
  return <AuthScreen defaultSignUp />;
};

export default Register;
